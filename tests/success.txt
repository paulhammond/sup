prep remote.db
sup config.hcl remote.db
cmp stdout output.txt
! stderr .

-- config.hcl --
source = "src"

-- output.txt --
local files:
a.txt
b.txt
remote files:
a.txt
-- src/a.txt --
a
-- src/b.txt --
b