# prep
prep remote.db

# upload
sup config.hcl remote.db
cmp stdout output.txt
! stderr .

# check
sup config.hcl remote.db
cmp stdout output2.txt
! stderr .

-- config.hcl --
source = "src"

-- output.txt --
local files:
a.txt
b.txt
c.txt
applying filters:
done
remote files:
a.txt
b.txt
d.txt
upload:
a.txt
c.txt
delete:
d.txt
uploading:
a.txt [123ms]
c.txt [123ms]
done
-- output2.txt --
local files:
a.txt
b.txt
c.txt
applying filters:
done
remote files:
a.txt
b.txt
c.txt
d.txt
upload:
delete:
d.txt
-- src/a.txt --
a
-- src/b.txt --
b
-- src/c.txt --
c